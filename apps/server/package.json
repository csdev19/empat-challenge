{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify-whitespace --minify-syntax --target bun-linux-x64 --outfile server src/index.ts",
    "dev": "bun run --hot src/index.ts",
    "dev:express": "bun run --hot src/express-server.ts",
    "start": "bun run src/index.ts",
    "start:express": "bun run src/express-server.ts",
    "start:prod": "./server"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.3.3",
    "@empat-challenge/auth": "workspace:*",
    "@empat-challenge/db": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.1",
    "elysia": "catalog:",
    "express": "^4.18.2",
    "ws": "^8.16.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@empat-challenge/config": "workspace:*",
    "@empat-challenge/domain": "workspace:*",
    "@types/bun": "catalog:",
    "@types/express": "^4.17.21",
    "@types/ws": "^8.5.10",
    "tsdown": "^0.16.5",
    "typescript": "^5"
  }
}
